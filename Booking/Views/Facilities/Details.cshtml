@model Booking.Data.Entities.Facility

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Details</h1>

<div>
    <h4>Facility</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Mobile)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Mobile)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Address)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Address)
        </dd>
    </dl>
</div>

<div class="d-flex justify-content-start gap-3 mt-4">
    <!-- Edit Button -->
    <a href="@Url.Action("Edit", "Facilities", new { id = Model?.Id })" class="btn btn-warning btn-md" style="width: 150px; height: 50px; display: flex; flex-direction: column; justify-content: center; align-items: center; border-radius: 10px;">
        <i class="fas fa-edit"></i> <!-- Font Awesome icon -->
        <span>Edit</span>
    </a>

    <!-- Back to List Button -->
    <a href="@Url.Action("Index", "Facilities")" class="btn btn-secondary btn-md" style="width: 150px; height: 50px; display: flex; flex-direction: column; justify-content: center; align-items: center; border-radius: 10px;">
        <i class="fas fa-arrow-left"></i> <!-- Font Awesome icon -->
        <span>Back to List</span>
    </a>

    <!-- Delete Button -->
    <form asp-action="Delete" asp-route-id="@Model?.Id" method="post" style="display: inline;">
        <button type="submit" class="btn btn-danger btn-md" style="width: 150px; height: 50px; display: flex; flex-direction: column; justify-content: center; align-items: center; border-radius: 10px;">
            <i class="fas fa-trash"></i> <!-- Font Awesome icon -->
            <span>Delete</span>
        </button>
    </form>
</div>
